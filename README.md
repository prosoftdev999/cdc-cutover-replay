# CDC Cutover Replay

A reproducible project for testing CDC (Change Data Capture) cutover and replay behavior.

The project focuses on validating event ordering, replay correctness, recovery behavior, and consistent state transitions during a CDC migration or cutover.

## Project Structure

```text
cdc-cutover-replay/
├── environment/
├── solution/
├── tests/
├── instruction.md
└── task.toml
